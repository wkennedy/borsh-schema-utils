{
  "name": "unborsh-wasm-dev",
  "version": "0.1.0",
  "description": "Development package for unborsh-wasm",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "clean": "rm -rf dist pkg pkg-node pkg-web target",
    "test": "wasm-pack test --node",
    "serve": "cd dist/examples && npx serve",
    "demo": "npm run build && npm run serve",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}